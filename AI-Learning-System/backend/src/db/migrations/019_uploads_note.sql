-- NOTE: On Render free tier, the filesystem is ephemeral.
-- Files in uploads/ are lost on redeploy.
-- This is a known limitation. For production, migrate to S3-compatible storage.
-- No schema change — this file exists as a reminder.
SELECT 1;
