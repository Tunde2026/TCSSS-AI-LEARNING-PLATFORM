# Tool IO schema

This file is a placeholder documenting the shape expected for tool registrations.

```json
{ "name": "example_tool", "description": "What the tool does", "inputSchema": { "type": "object" }, "execute": "async function" }
```

No real tools are defined yet.
